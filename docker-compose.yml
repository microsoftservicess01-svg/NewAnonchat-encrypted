version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "10000:10000"
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:10000
      - VITE_SIGNALING_URL=http://localhost:10000
